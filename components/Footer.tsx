export default function Footer() {
  return (
    <footer className="mx-auto max-w-3xl px-6 py-10">
      <p className="font-mono text-xs text-faint">
        # built with next.js — last deployed {new Date().getFullYear()}
      </p>
    </footer>
  );
}
